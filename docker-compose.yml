version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: taskflow_mongodb
    ports:
      - "27017:27017"
    volumes:
      - taskflow_db_data:/data/db
    networks:
      - taskflow_network

networks:
  taskflow_network:
    driver: bridge

volumes:
  taskflow_db_data:
